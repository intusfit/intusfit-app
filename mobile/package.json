{
  "name": "intus-fit-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper nativo iOS e Android do aplicativo do aluno Intus Fit.",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "sync": "npm run build:web && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1",
    "typescript": "^7.0.2"
  }
}
