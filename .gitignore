# Credenciais - nunca versionar
app/config/db.php
admin/config.php
app/config/gdrive.json

# Backups automáticos do G4OS e backups manuais antigos - a partir de agora o Git é o backup
*.g4os-backup-*
*-old-*
*-bug-*

# Conteúdo/uploads que o painel admin escreve em produção - deploy via git não deve sobrescrever
admin/content.json
admin/data.json
admin/pages/
admin/templates/
img/
app/img/

# Logs de runtime
app/logs/

# Pasta antiga desativada (renomeada, mantida so por precaucao)
app/_removido-p-8f3k2-20260905/
